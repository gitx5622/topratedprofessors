import React from "react";
import SettingCard from "./setting-card";

const SettingView = ({ section }) => {
  return (
    <div>
      <SettingCard section={section} />
    </div>
  );
};

export default SettingView;
