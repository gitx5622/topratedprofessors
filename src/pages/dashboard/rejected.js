import React from 'react';
import Dashboard from "../../components/admin/dashboard";

const Rejected = () => {
    return (
        <Dashboard page='dashboard' section='rejected'/>
    );
};

export default Rejected;