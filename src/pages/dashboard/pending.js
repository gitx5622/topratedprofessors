import React from 'react';
import Dashboard from "../../components/admin/dashboard";

const Pending = () => {
    return (
        <Dashboard page='dashboard' section='pending'/>
    );
};

export default Pending;