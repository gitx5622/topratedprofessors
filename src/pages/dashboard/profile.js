import React from 'react';
import Dashboard from "../../components/admin/dashboard";

const Profile = () => {
    return (
        <Dashboard page='dashboard' section='profile'/>
    );
};

export default Profile;